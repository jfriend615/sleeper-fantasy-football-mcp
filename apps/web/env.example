# MCP Authentication Token
# Generate a secure random string for production
#
# Easy methods to generate a secure token:
# 1. Node.js one-liner: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 2. OpenSSL: openssl rand -hex 32
# 4. Manual: Use any password manager's password generator (32+ chars)
#
MCP_AUTH_TOKEN=your-secure-token-here

# Optional: Redis/Upstash for caching (if you add it later)
# REDIS_URL=redis://localhost:6379
# UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your-token

# Optional: Vercel Blob for persistent storage
# BLOB_READ_WRITE_TOKEN=your-blob-token
